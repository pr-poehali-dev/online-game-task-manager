# ==== Секреты приложения ====
# Скопируйте этот файл в .env и заполните реальными значениями.
# НИКОМУ не показывайте .env и не коммитьте его в git.

# Подключение к PostgreSQL (host обычно localhost, если БД на этом же сервере)
DATABASE_URL=postgresql://era_user:ВАШ_ПАРОЛЬ@localhost:5432/era_db

# Имя схемы в базе. Если не знаете — оставьте public
MAIN_DB_SCHEMA=public

# Доступ к S3-хранилищу (MinIO или внешний Object Storage)
AWS_ACCESS_KEY_ID=ВАШ_КЛЮЧ
AWS_SECRET_ACCESS_KEY=ВАШ_СЕКРЕТ

# Своё S3-совместимое хранилище (MinIO). Если не задать — используется облако poehali (не будет работать на вашем сервере).
S3_ENDPOINT=http://127.0.0.1:9000
S3_BUCKET=files
# Публичный адрес, по которому отдаются загруженные файлы (например через Nginx location /files)
S3_PUBLIC_URL=https://ваш-домен.ру/files

# Telegram-бот (создаётся у @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC-ВАШ_ТОКЕН
TELEGRAM_BOT_USERNAME=era_task_bot

# Адрес вашего сайта (для ссылок в уведомлениях). Без слэша в конце
APP_URL=https://ваш-домен.ру

# Секретный ключ для защиты функции проверки дедлайнов (backend/deadline-reminders)
# от вызовов посторонними. Внешний cron-сервис должен передавать его в заголовке
# X-Cron-Secret. Придумайте любую длинную случайную строку (например через
# `openssl rand -hex 32` в терминале) и впишите то же самое значение в настройках
# вашего cron-сервиса (cron-job.org и т.п.).
CRON_SECRET=ВАШ_СЛУЧАЙНЫЙ_КЛЮЧ

# Путь к папке backend (обычно менять не нужно)
BACKEND_DIR=/var/www/era/backend
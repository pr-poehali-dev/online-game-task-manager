import type { Article } from './shared';

// Кеш полных статей по ID на время сессии (тот же принцип, что и taskDataCache.ts для задач) —
// KnowledgeBase остаётся смонтированным на весь сеанс (см. IndexMain.tsx, паттерн visited), но
// открытие статьи внутри неё переключает локальный state current/null через ссылку в URL
// (/kb/:id), и при каждом повторном открытии той же статьи (список → статья → список → та же
// статья) без кеша происходил бы повторный fetch и спиннер. Кеш точечно обновляется при
// сохранении/переключении избранного, поэтому не протухает в рамках сессии.
export const articleCache = new Map<string, Article>();

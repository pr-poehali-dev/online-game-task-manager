-- Новый режим раздела AI: 'document' — сборка готовых Excel/Word по текстовому запросу
-- (backend/ai/documents.py). Ограничения CHECK на ai_chats.mode нет, поэтому значение пишется
-- без изменения схемы — обновляем только комментарий, чтобы список режимов не расходился
-- с реальностью при чтении структуры БД.
COMMENT ON COLUMN ai_chats.mode IS
    'Режим диалога: chat — обычный текстовый чат, code — помощь с кодом (свой системный промпт), image — генерация изображений, video — генерация видео, document — сборка Excel/Word файлов.';
# Веб-обёртка
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.2.0

# Зависимости самих функций
psycopg2-binary>=2.9.0
# boto3>=1.36.0 нужен, чтобы работал параметр отключения checksum-трейлера в backend/*/index.py
# (Config request_checksum_calculation/response_checksum_validation) — без него старые версии
# botocore либо ломают загрузку файлов "мусором" в конце, либо (на совсем старых) код всё равно
# устойчив благодаря try/except в _S3_CONFIG, но лучше сразу ставить актуальную версию.
boto3>=1.36.0
# paramiko нужен backend/patches (SSH/SFTP-загрузка патчей на сервер лаунчера) и backend/logs
# (SFTP-фолбэк чтения файлов логов, если не настроена база era_logs с индексатором).
# server.py грузит ВСЕ функции backend при старте — без этого пакета падает весь backend,
# а не только загрузка патчей/логов.
paramiko>=3.4.0
# openpyxl и python-docx нужны backend/ai (раздел "AI" → режим "Документ": сборка Excel/Word
# файлов моделью по запросу сотрудника, см. backend/ai/documents.py). Без них падает весь
# backend при старте (server.py грузит все функции разом), а не только раздел AI.
openpyxl>=3.1.0
python-docx>=1.1.0
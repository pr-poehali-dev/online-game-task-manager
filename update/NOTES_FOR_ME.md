# Заметка себе (не для пользователя)

⚠️ КЛЮЧЕВОЙ КОНТЕКСТ ПРОЕКТА (18 сентября): разработка ведётся НЕ ДЛЯ
poehali.dev — это self-hosted проект пользователя на его боевом ВПС
(домен forge.la2era.com, self-hosted `deploy/server.py` на FastAPI,
запущен как systemd-сервис). Здесь, в облачной среде poehali.dev, я
только пишу и тестирую код, а реально работает он у пользователя на
СВОЁМ сервере — доставляется патчами через `update/ДАТА/` (см. правила
ниже), не через git/CI/CD.

Из этого следует важный вывод про архитектурные ограничения: то, что
НЕВОЗМОЖНО в облачных функциях poehali.dev (например честный token-by-
token SSE-стриминг ответа ИИ — там жёсткий контракт handler(event,
context) -> dict, весь ответ одним куском, из-за таймаута и биллинга
по compute_seconds = вызовы × таймаут), может быть ВПОЛНЕ РЕАЛИЗУЕМО на
боевом ВПС пользователя, потому что там `deploy/server.py` — обычный
FastAPI-процесс, который FastAPI умеет отдавать `StreamingResponse` и
держать соединение открытым сколько нужно, без таймаута облачной
функции и без биллинга за compute_seconds. ВЫВОД НА БУДУЩЕЕ: никогда не
отвечать "это невозможно" про какую-либо возможность backend, опираясь
ТОЛЬКО на ограничения облачных функций poehali.dev — сначала проверять,
не снимается ли это ограничение на self-hosted `deploy/server.py`
пользователя, и явно разделять "невозможно здесь, в облаке, при
разработке" от "невозможно у пользователя на боевом сервере". Если
фича реализуема только на self-hosted стороне — она всё равно
собирается и тестируется здесь как обычно, но упаковывается в
`update/ДАТА/` с пометкой в README, что раскрывается она в полную силу
именно на self-hosted (например SSE-стриминг сквозь облачную функцию
poehali.dev всё ещё не будет "живым" в момент разработки/превью — это
нормально, важен код, который ляжет на ВПС).

⚠️ ВСЕГДА проверять и заменять `poehali.dev`/`cdn.poehali.dev`/
`bucket.poehali.dev` на боевые значения проекта, прежде чем класть файлы
в `update/ДАТА/`:

- `endpoint_url=os.environ.get('S3_ENDPOINT', 'https://bucket.poehali.dev')`
  → `endpoint_url=os.environ.get('S3_ENDPOINT', 'http://127.0.0.1:9000')`
- `_public_url()` с хардкодом `https://cdn.poehali.dev/projects/{AWS_ACCESS_KEY_ID}/bucket/{key}`
  → паттерн с приоритетом `S3_PUBLIC_URL` / `CDN_BASE_URL`, запасной вариант
  `http://{S3_ENDPOINT}/{S3_BUCKET}/{key}` (см. `backend/tasks/index.py`
  как эталон — уже приведён к этому виду).

Боевой домен пользователя: **forge.la2era.com** (использовать в комментариях
вида `# https://forge.la2era.com/files/<key>`).

Проверять командой перед тем как сказать пользователю "готово":
```
grep -rn "poehali.dev" backend/ update/ДАТА/ | grep -v __pycache__ | grep -v func2url.json
```
(`func2url.json` и `backend/dev-login`/`RESEARCH_NOTES.md` — норма, это
служебные вещи платформы/заметки, не боевой S3-код).

Файлы, где эта проблема уже встречалась и была исправлена 30 июля:
`backend/patches/index.py`, `backend/admin/index.py`, `backend/tasks/index.py`,
`backend/knowledge/index.py`, `backend/ideas/index.py` (+ их копии в
`update/2026-07-30/backend/...`, кроме knowledge/ideas — они не входили в
этот апдейт).

3 августа (`update/2026-08-03/`) та же проверка повторена и пройдена для
всех скопированных backend-файлов — исправлены `backend/patches/index.py`,
`backend/admin/index.py` (только endpoint_url — там нет своего `_public_url`,
использует общий паттерн иначе), `backend/tasks/index.py`,
`backend/knowledge/index.py`, `backend/ideas/index.py`. Новые файлы
`backend/service-keys/index.py` и `backend/storage-config/index.py` S3 не
используют вовсе (сервисные разделы кабинета), хардкода в них нет. Оставшиеся
упоминания `poehali.dev` в `backend/storage-config/index.py` и
`src/pages/cabinet/CabinetStorage.tsx` — осознанные текстовые пояснения для
пользователя про разницу облачного/self-hosted окружения, не адреса
запросов — трогать не нужно.

Также с 30 июля появился новый паттерн self-hosted-only функционала:
`backend/storage-config` пишет/читает `.env` на диске VPS (ключ MANAGED_KEYS)
и требует ручной настройки systemd path-unit на сервере пользователя
(`deploy/era-backend-env.path`/`.service`) — сама функция НЕ может
перезапустить backend (subprocess запрещён в облачных функциях), поэтому
всегда описывать этот шаг явно в README апдейта, не полагаться, что
пользователь сам найдёт эти файлы.

⚠️ 5 августа: две новые категории проблем self-hosted-сборки/запуска,
проверять КАЖДЫЙ раз перед тем как отдать `update/ДАТА/`:

1. **Новые pip-зависимости отдельных backend-функций не попадают в
   `deploy/requirements.txt`.** У self-hosted `deploy/server.py` грузит ВСЕ
   функции backend при старте одним махом — если хоть одна не импортируется
   (ModuleNotFoundError), падает ВЕСЬ backend, а не только эта функция.
   Пример: `backend/patches/index.py` использует `paramiko` (SSH/SFTP на
   лаунчер) — это было в `backend/patches/requirements.txt`, но не в общем
   `deploy/requirements.txt`, из-за чего весь backend не стартовал у
   пользователя (включая вход через бота, хотя вход тут вообще ни при чём).
   Проверять командой:
   ```
   for f in backend/*/requirements.txt; do cat "$f"; done | sort -u
   ```
   и сверять каждый пакет с `deploy/requirements.txt` — если чего-то не
   хватает, дописывать туда с комментарием, какая функция его требует.

2. **`src/pages/Login.tsx` и любые другие "боевые" страницы НЕ должны
   импортировать dev-only компоненты, которых нет в `update/`.**
   `DevOnlyLoginButton` (кнопка тестового входа, видна только на
   `*.poehali.dev`) и backend `dev-login` — оба намеренно НЕ переносятся в
   `update/` (см. комментарии в самих файлах: "НИКОГДА не переносить").
   Но исходный `src/pages/Login.tsx` жёстко их импортирует — из-за этого
   `npm run build` падает у любого self-hosted пользователя с
   `Could not load src/components/DevOnlyLoginButton`.
   Перед каждым апдейтом: копия `update/ДАТА/src/pages/Login.tsx` должна
   НЕ содержать `import DevOnlyLoginButton` и `<DevOnlyLoginButton .../>`
   (и связанный с ним `useAuth`/`applySession`, если больше нигде на
   странице не используется). Проверять командой:
   ```
   grep -rn "DevOnlyLoginButton\|from '@/lib/auth'.*useAuth" update/ДАТА/src/pages/Login.tsx
   ```
   Общее правило: любой файл с пометкой "не переносить в update/" в
   комментарии — грепать по всем файлам, которые ЕГО импортируют, и в
   копии внутри `update/ДАТА/` вручную вырезать импорт/использование,
   а не просто не копировать сам файл.

   ✅ 6 августа: пользователь подтвердил, что уже вручную перенёс это
   исправление (`update/2026-08-03/src/pages/Login.tsx` без
   `DevOnlyLoginButton`) на свой боевой сервер — на сегодня тема закрыта,
   в новых апдейтах отдельно про неё напоминать не нужно. Но правило выше
   (грепать импорты dev-only файлов перед КАЖДЫМ новым апдейтом) остаётся
   в силе — это защита на будущее, если `DevOnlyLoginButton` снова окажется
   где-то заимпортирован в боевом файле по ошибке.

⚠️ 6 августа: добавлен вывод технического кода ошибки (`code`, например
`ssh_error_AuthenticationException`) в текст сообщения об ошибке заливки/
сверки с лаунчером (`src/pages/index/usePatches.ts`, `handleLauncherUpload`/
`handleLauncherSync`) — раньше при неразобранных явно кодах ошибка
показывала только дженерик-текст "проверьте подключение", и пользователь
без доступа к серверным логам backend не мог понять, в чём именно проблема
(неверный SSH-хост, неверный пароль, нет прав на запись и т.п.). Учтено в
`update/2026-08-06/`. Если в будущем понадобится вывести отладочную
информацию куда-то ещё в интерфейсе (например для storage-config или
service-keys) — использовать тот же паттерн: не прятать код ошибки от
backend, а дописывать его в конец пользовательского сообщения в скобках.

✅ 6 августа (вечер): `update/2026-08-06/` пересобран целиком и включает все
три фичи дня одним пакетом (README.md объединяет все разделы):
1. Свой никнейм/аватарка участника в кабинете, не зависящие от Telegram
   (новые колонки `users.nickname`/`users.avatar_url`, миграция V0074,
   новые actions в `backend/auth`: `set_nickname`/`upload_avatar`/
   `remove_avatar` — доступны самому пользователю без прав администратора).
2. Переименование/удаление ПРОИЗВОЛЬНОЙ вложенной папки в дереве «Патчи»
   (новые actions `rename_folder`/`delete_folder` в `backend/patches`,
   работают по префиксу пути в отличие от старых `rename_root`/
   `delete_root`, которые остались только для корневых папок).
3. Журнал активности (кабинет → «Журнал») теперь фиксирует действия с
   файлами патчей — новые ключи в `ACTIVITY_META`
   (`src/pages/admin/adminShared.ts`) и вызовы `_log_activity` по всему
   `backend/patches/index.py`.

Проверено перед сборкой: `backend/dev-login` НЕ включён (dev-only, как и
предписано правилом выше). `backend/auth/requirements.txt` пополнился
`boto3` (нужен для загрузки аватарки в S3) — учтено в README раздела 1,
но `boto3` уже есть в общем `deploy/requirements.txt`, доп. действий на
сервере пользователя не требуется. Новых pip-зависимостей, которых нет
в `deploy/requirements.txt`, не добавилось.

❌→✅ ИСПРАВЛЕНО (6 августа, поздний вечер, по замечанию пользователя): при
первой сборке `update/2026-08-06/` я ОШИБОЧНО решил, что default-значения
`endpoint_url=os.environ.get('S3_ENDPOINT', 'https://bucket.poehali.dev')` и
`https://cdn.poehali.dev/projects/{AWS_ACCESS_KEY_ID}/bucket/{key}` в
`backend/auth/index.py`, `backend/admin/index.py`, `backend/patches/index.py`
— это "осознанный safety-default, трогать не нужно". Это НЕВЕРНО и прямо
противоречит правилу в самом начале этого файла ("ВСЕГДА проверять и
заменять poehali.dev/... на боевые значения ПЕРЕД тем как класть файлы в
update/ДАТА/") — эталон уже был готов с 3 августа в
`update/2026-08-03/backend/tasks/index.py` (`http://127.0.0.1:9000` и
`http://{S3_ENDPOINT}/{S3_BUCKET}/{key}`), но я не сверился с ним при копи-
ровании файлов вечером 6 августа. Исправлено во всех трёх файлах на
эталонный паттерн. ВЫВОД НА БУДУЩЕЕ: команда для проверки
(`grep -rn "poehali.dev" backend/ update/ДАТА/ | grep -v __pycache__ | grep
-v func2url.json`) должна запускаться КАЖДЫЙ раз перед тем как сказать
пользователю "апдейт готов" — а если она находит совпадения в
`update/ДАТА/backend/*/index.py`, это ВСЕГДА ошибка, требующая
исправления, а не повод для рационализации задним числом, что "так и
должно быть". Единственное законное исключение — упоминание poehali.dev в
человекочитаемом ТЕКСТЕ (комментарий/докстрока/README), где речь идёт про
сам редактор/платформу, а не про адрес запроса в коде.

✅ 6 августа (поздний вечер): в `update/2026-08-06/` добавлена 4-я тема дня —
скачивание отмеченных чекбоксами файлов одним архивом (`action='zip_bulk'`
в `backend/patches`, кнопка «Скачать архивом» рядом с «Удалить выбранное» в
`PatchesTree.tsx`) и чекбокс на КАЖДОЙ папке дерева (`PatchesTreeFolder.tsx`,
использует новую `collectFilePaths()` из `patchesUtils.ts` — рекурсивно
собирает пути всех файлов внутри узла, включая вложенные подпапки;
`toggleSelectFolder` в `usePatches.ts` отмечает/снимает их разом). Раздел 4
README добавлен между разделом 3 (журнал активности) и прежним разделом 4
(код ошибки лаунчера, который стал разделом 5) — нумерация везде сквозная.
При сборке из апдейта убраны файлы, которые сегодня физически не менялись
(`PatchesTreeFileRow.tsx`, `PatchesToolbar.tsx`, `patchesApi.ts` — были
скопированы по инерции вместе с остальным `src/pages/index/`, но не входили
в `git show f78df12 --stat`) — держать в апдейте только реально изменённые
файлы, чтобы diff для пользователя оставался маленьким и понятным. Общее
правило на будущее: перед копированием файла в `update/ДАТА/` сверяться с
`git log --oneline --since="ДАТА 00:00" -- путь/к/файлу`, а не копировать
директорию целиком по памяти о том, что "там же рядом лежат правки".

✅ ИТОГ по сессии 6 августа (закрыто, коммит `98237a2`): цикл "билд →
пользователь заметил хардкод poehali.dev в `update/2026-08-06/backend/{admin,
auth,patches}/index.py` → исправлено на `127.0.0.1:9000`/`S3_ENDPOINT`+
`S3_BUCKET` паттерн → зафиксировано здесь" пройден и закрыт. Проверка
`grep -rn "poehali.dev" update/2026-08-06/ | grep -v __pycache__ | grep -v
func2url.json` на момент закрытия сессии даёт ТОЛЬКО одну строку —
текстовое пояснение про редактор poehali.dev в `README.md` (не адрес
запроса), это норма. state `update/2026-08-06/` актуален и содержит все
4 темы дня + этот S3-фикс. Если в новой сессии откроется правка
`usePatches.ts`/`patchesUtils.ts`/`Patches.tsx`/`patchesApi.ts` (эти файлы
были показаны системой как "текущее содержимое после билда") — это ЗНАЧИТ,
что где-то в текущем диалоге шла/пойдёт очередная правка фичи "Патчи"
(скачивание архивом/выбор папки), а не что появилась новая проблема —
сверяться с содержимым этой заметки и с `update/2026-08-06/README.md`
перед тем как обновлять копии в `update/`.

✅ 22 августа: собран `update/2026-08-22/` — САМЫЙ КРУПНЫЙ апдейт на сегодня
(~96 файлов), точка отсчёта — коммит `98237a2` (последний вошедший в
`update/2026-08-06/`), диапазон `98237a2..HEAD` = 81 коммит. Две большие
независимые темы + мобильные правки:
1. Раздел «Логи» переведён на PostgreSQL с опциональным self-hosted
   индексатором (`logs-indexer/`, отдельный процесс НЕ через основной
   backend/server.py — свой systemd timer на хостинге логов). SFTP-режим
   остался рабочим фолбэком (переключение автоматическое, по наличию
   заполненного `LOGS_DB_URL` в service_keys). Миграция V0075.
2. Полностью новый раздел «AI» (backend/ai/ — 9 файлов, 26 файлов фронтенда
   Ai*.tsx/ai*.ts/useAi*.ts) — общение с ИИ-моделями через AI Tunnel
   (aitunnel.ru), включая генерацию картинок/видео и сборку Excel/Word
   документов. Миграции V0076–V0081.

⚠️ НАХОДКА (важно на будущее): миграции V0078–V0081 в исходном коде
содержали ЖЁСТКО прописанное имя схемы редактора
(`t_p84024572_online_game_task_man.table_name` вместо просто
`table_name`) — единственный раз, когда это встретилось в новых
миграциях с 6 августа (V0075/V0076/V0077 написаны правильно, без схемы,
полагаются на `search_path`). На self-hosted сервере пользователя такой
схемы не существует — миграция упала бы с `schema does not exist`. Исправлено
через `sed -i 's/t_p84024572_online_game_task_man\.//g'` по всем четырём
файлам в `update/2026-08-22/db_migrations/`. ПРАВИЛО НА БУДУЩЕЕ: перед
копированием КАЖДОЙ новой миграции в `update/ДАТА/` проверять
`grep -n "t_p84024572" db_migrations/V0XXX*.sql` (или актуальное имя схемы
проекта, если оно другое) — если находит, вырезать, оставляя миграцию
опирающейся на `search_path`, как это уже сделано во всех остальных
корректных миграциях проекта и как ожидает `deploy/apply_migrations.sh`.

Хардкоды poehali.dev исправлены в трёх местах (тот же паттерн, что 6
августа): `backend/ai/common.py` (`_s3_client`/`_public_url` — там уже
был частично правильный fallback на `S3_PUBLIC_URL`/`CDN_BASE_URL`, но
дефолт `S3_ENDPOINT` всё ещё указывал на `bucket.poehali.dev`), 
`backend/logs/game_lookup.py` и `backend/admin/index.py` (там появилось
новое использование S3 в контексте новой фичи «AI usage summary» —
`backend/admin/index.py` НЕ фигурировал в списке файлов с этой проблемой
за 30 июля/3 августа, значит хардкод в нём — свежий, добавленный между
6 и 22 августа, нужно ВСЕГДА перепроверять весь файл целиком, а не
полагаться на память о том, что "этот файл уже когда-то чинили").
`logs-indexer/game_lookup.py` — ОТДЕЛЬНАЯ копия модуля для self-hosted
индексатора, она с самого начала написана правильно (дефолт
`http://127.0.0.1:9000`, есть explicit-комментарий в коде почему) — её
трогать не нужно, легко перепутать с `backend/logs/game_lookup.py`
(тёзка, но разные файлы с разными дефолтами по замыслу).

Проверка `grep -rn "poehali.dev" update/2026-08-22/ | grep -v __pycache__
| grep -v func2url.json` на момент закрытия темы даёт только текстовые
упоминания в комментариях/докстроках/README/RESEARCH_NOTES.md — ни одного
адреса в коде запроса. Это норма (см. правило в начале файла).

Зависимости: `deploy/requirements.txt` дополнен `openpyxl`+`python-docx`
(нужны `backend/ai/documents.py` для сборки Excel/Word) — `psycopg2-binary`/
`boto3`/`paramiko` уже были. `logs-indexer/requirements.txt` — ОТДЕЛЬНЫЙ
процесс со своим venv на хостинге логов, к `deploy/requirements.txt`
основного backend отношения не имеет, туда его пакеты сверять не нужно.
`package.json` уже содержит новые фронтенд-зависимости
(`react-markdown`/`react-syntax-highlighter`/`remark-gfm`) — скопирован
целиком, `npm install` подтянет сам.

`src/pages/Login.tsx` НЕ менялся в диапазоне `98237a2..HEAD` (проверено
`git log --oneline 98237a2..HEAD -- src/pages/Login.tsx` — пусто) — в
`update/2026-08-22/` не включён, актуальная копия без `DevOnlyLoginButton`
у пользователя уже есть с 6 августа (см. запись выше от того дня).

Важный архитектурный момент, который стоит держать в голове при любой
будущей правке этих разделов: разработка «Логов» и «AI» шла в ОДНОЙ
сессии и физически смешала правки в ряде общих файлов — `src/App.tsx`,
`src/pages/Index.tsx`, `src/pages/index/IndexMain.tsx`,
`src/pages/index/IndexTopbar.tsx`, `src/pages/index/IndexSidebar.tsx`,
`src/pages/index/sharedHelpers.ts`, `src/pages/index/sharedTypes.ts`,
`src/lib/auth.tsx`, `backend/admin/index.py`, `src/pages/admin/adminShared.ts`
содержат ОДНОВРЕМЕННО изменения обеих фич — их нельзя аккуратно разделить
построчно на "только логи"/"только AI", поэтому README `update/2026-08-22/`
явно предупреждает копировать актуальную версию каждого такого файла
целиком независимо от того, переносит пользователь одну тему или обе.
✅ 14 сентября: собран `update/2026-09-14/` — 68 файлов, точка отсчёта `a204a75`
(коммит сборки `update/2026-08-22/`), диапазон `a204a75..HEAD` = 32+ коммита.
Пять тем: файлы/лимит объёма в AI, журнал ошибок AI, блок доработок таск-менеджера
(несколько серверов у задачи и спринта, право `task_archive`, автор закрытия,
сортировка+фильтр архива, обязательная категория), редактируемые статусы деплоя
(`deploy_statuses`, V0095) и переработка оформления (радиусы от одной переменной,
премиальная тёмная тема).

⚠️ ГЛАВНАЯ ОСОБЕННОСТЬ ЭТОЙ СБОРКИ (по прямому указанию пользователя): в диапазон
коммитов попала разработка «Проектов» в разделе AI, которая в тот же день была
СВЁРНУТА (коммит `4d46123`, миграция V0092 в основном репозитории). В апдейт
НЕ должны попадать её хвосты. Что сделано:
- Из апдейта ИСКЛЮЧЕНЫ миграции V0084, V0085, V0086, V0087, V0089, V0090, V0092 —
  все они целиком про «Проекты» (таблицы `ai_projects`/`ai_file_chunks`, автосводка,
  сброс пустых сводок, сама миграция вывода проектов). На боевом сервере этих таблиц
  нет и не будет, поэтому и «миграция отката» V0092 там не нужна — она бы просто
  упала на `UPDATE ai_chats SET project_id = NULL` (нет колонки), а
  `deploy/apply_migrations.sh` идёт с `ON_ERROR_STOP=1` и уронил бы ВСЮ накатку.
  Нумерация в апдейте идёт с пропусками — это зафиксировано врезкой в README.
- V0088 (`ai_files.rel_path`) НУЖНА (код `backend/ai/common.py`/`userfiles.py` пишет и
  читает `rel_path`), но оригинал содержал
  `CREATE INDEX ... ON ai_files(project_id, rel_path)` — колонки `project_id` на
  боевом сервере нет. Файл ПЕРЕПИСАН вручную: индекс теперь по `(user_id, rel_path)`,
  комментарии без упоминания проектов.
- V0091 (`ai_error_log`) — из DDL удалена колонка `project_id INTEGER`.
- V0082/V0083 — переписаны комментарии: лимит по КОЛИЧЕСТВУ файлов был отменён позже
  в этом же диапазоне (остался только объём, и он считает ВСЕ файлы, включая
  сгенерированные). Колонка `users.ai_file_limit` в V0082 оставлена (код мог бы
  споткнуться о её отсутствие при откате), но помечена как НЕ ИСПОЛЬЗУЕТСЯ.
- `docs/ai-section-overview.md` ИСКЛЮЧЁН из апдейта целиком: файл наполовину описывает
  «Проекты» (пометка «история» в шапке). Это внутренний документ для разработки, а не
  инструкция по установке — в апдейте пользователю он не нужен и только путал бы.
- В `src/pages/index/AiChatList.tsx` и `AiChatPane.tsx` вычищены осиротевшие
  комментарии про проекты (в `AiChatPane.tsx` комментарий «Название проекта, если это
  сессия проекта» висел вообще без относящегося к нему поля).
ПРАВИЛО НА БУДУЩЕЕ: если в диапазоне коммитов есть свёрнутая фича, недостаточно
исключить её файлы — надо проверять КАЖДУЮ переносимую миграцию на ссылки
(колонки/таблицы) свёрнутой фичи и грепать перенесённый код по её ключевым словам.
Проверка для этой сборки:
`grep -rln "ai_projects\|ai_file_chunks\|project_id\|ai_project_limit" update/ДАТА/`
должна давать пусто.

Стандартные проверки пройдены:
- poehali.dev: найден и исправлен в ЧЕТЫРЁХ файлах — `backend/admin/index.py`,
  `backend/auth/index.py`, `backend/tasks/index.py`, `backend/ai/common.py`
  (`endpoint_url` → `http://127.0.0.1:9000`; `_public_url` fallback → сборка из
  `S3_ENDPOINT` + `S3_BUCKET` вместо `cdn.poehali.dev/projects/...`). Обратить
  внимание: `backend/tasks/index.py` был ЭТАЛОНОМ с 3 августа, но в него с тех пор
  вернулся облачный fallback — значит «этот файл уже чинили» НЕ основание пропускать
  проверку (та же ошибка, о которой запись от 6 августа).
- Имя схемы редактора в новых миграциях: не найдено, все опираются на `search_path`.
- Зависимости: `backend/ai/requirements.txt` пополнился `pypdf` — дописан в
  `deploy/requirements.txt` (в коде импорта пока нет, но `server.py` грузит все функции
  разом). Внимание: в конце `deploy/requirements.txt` не было перевода строки, из-за
  чего `>>` слепил строки — проверять хвост файла после дописывания.
  `gmpy2` (в `backend/patches/requirements.txt`) в `deploy/requirements.txt` намеренно
  НЕ добавлен: в `l2encdec.py` он под `try/except ImportError` с рабочим fallback.
- Dev-only: `backend/dev-login` не включён, `DevOnlyLoginButton` в апдейте не
  встречается, `src/pages/Login.tsx` в диапазоне не менялся и не включён.
- `package.json` в диапазоне НЕ менялся — новых npm-зависимостей нет,
  в апдейт не включён.

✅ 15 сентября: собран `update/2026-09-15/` — 36 файлов, точка отсчёта `85db8cf`
(коммит сборки `update/2026-09-14/`), диапазон `85db8cf..HEAD` = 4 коммита.
Две темы: переименование раздела «К рестарту» → «На лайв» и вторая волна оформления
(акцентный градиент+свечение, шкала иконок 12/14/16, единая кривая переходов,
скелетоны вместо крутилок, зерно и дрейф фона, премиальный вид модалок задач).

⚠️ Про переименование: в интерфейсе название лежит в коде, а тексты справки — В БАЗЕ
(`faq_items`). Правка только кода оставила бы рассинхрон: меню «На лайв», а справка
объясняет «К рестарту». Поэтому в апдейте есть миграция V0096 — строковый REPLACE по
`question`/`answer` (7 статей). Внутренний идентификатор раздела (`restart`) НЕ трогали:
на нём висят deep-links из Telegram и выданные права `task_restart`. Статус деплоя
«Можно заливать на лайв» — ОТДЕЛЬНАЯ сущность, намеренно не переименован (легко перепутать
с разделом, я сам в диалоге ошибочно назвал его «колонкой» — пользователь поправил).
ПРАВИЛО НА БУДУЩЕЕ: при любом переименовании раздела грепать не только код, но и БД
(`faq_items`, `kb_articles`) — иначе справка расходится с интерфейсом.

Стандартные проверки — ОБЕ дали улов, оба раза это были ровно те грабли, что описаны выше:
- poehali.dev: снова всплыл в `backend/tasks/index.py` (!) и добавился в
  `backend/patches/index.py`. `tasks/index.py` чинили уже ТРИЖДЫ (3 авг, 14 сент, теперь) —
  значит облачный дефолт возвращается в файл при правках через редактор. Проверку
  `grep -rn "poehali.dev" update/ДАТА/` НИКОГДА не пропускать, даже если «этот файл недавно
  чинили».
- `src/pages/Login.tsx` в этот раз ПОПАЛ в диапазон (правился по дизайну — знак+фон), и в
  нём снова живой `import DevOnlyLoginButton`. Сработало правило от 5 августа: в копии
  внутри `update/` вырезаны импорт, `useAuth`/`applySession` и сам вызов. Импорт
  `AuthUser` ОСТАВЛЕН — он ещё используется в `handleSuccess`. Сборка именно версии из
  апдейта проверена подменой файла (`cp` в src → `vite build` → откат) — проходит.
  Вывод: как только Login.tsx снова оказывается в диапазоне, этот шаг обязателен.

Зависимости: `package.json`, `deploy/requirements.txt`, `backend/*/requirements.txt` в
диапазоне НЕ менялись — в апдейт не включены, доустановка не нужна.
Схема редактора в V0096 не фигурирует (обычный UPDATE без имени схемы).

✅ 15 сентября (позже): в `update/2026-09-15/` ДОБАВЛЕНА 3-я тема — несколько спринтов у
задачи (миграция V0097 + 14 файлов). Патч был собран коммитом `1c6ce2e`, фича пришла
следом (`33ab865`), поэтому папка не пересобиралась с нуля, а дополнялась: скопированы
только файлы из диапазона `1c6ce2e..HEAD`, README получил раздел 3 и упоминание второй
миграции в шапке/порядке действий. Итого 43 файла, 2 миграции.

⚠️ ВАЖНЫЙ ПОВТОР: копирование поверх уже проверенной папки ОТМЕНЯЕТ прежние ручные
исправления в тех файлах, которые перезаписались. `backend/tasks/index.py` попал в новый
диапазон — и снова принёс облачные дефолты `bucket.poehali.dev`/`cdn.poehali.dev`,
хотя в этой же папке они были исправлены часом ранее. Исправлено повторно.
ПРАВИЛО: после КАЖДОГО досыпания файлов в уже готовый `update/ДАТА/` прогонять весь
блок проверок заново (poehali.dev / dev-only / схема), а не только смотреть на новые
файлы. Это уже четвёртый случай с `tasks/index.py`.

Архитектура фичи (на случай будущих правок): `tasks.sprint_ids` (jsonb) по образцу
`tasks.servers` (V0093) и `assignee_ids`; одиночное `sprint_id` СОХРАНЕНО и хранит первый
элемент списка. На фронте единая точка совместимости — `taskSprintIds()` в
`sharedHelpers.ts` (аналог `taskServerIds`). Два неочевидных места, которые пришлось
чинить отдельно: (1) привязка задач при создании спринта раньше делала
`SET sprint_id = %s` и ЗАТИРАЛА прежние спринты — теперь append через `||` с проверкой
`@>`; (2) при удалении спринта задачи оставались со ссылкой на несуществующий спринт —
добавлено вычитание `- %s` из jsonb и пересборка `sprint_id` из `sprint_ids->>0`.

✅ 15 сентября (ещё позже): в `update/2026-09-15/` ДОБАВЛЕНА 4-я тема — три кнопки в
RichEditor (горизонтальная линия, блок кода, выравнивание текста). Патч дополнялся уже
второй раз за день (после темы спринтов) — снова просто досыпаны 2 файла
(`src/components/RichEditor.tsx`, `src/index.css`) без пересборки всей папки. README
получил раздел 4 и уточнение в порядке действий про пакет text-align.

⚠️ ВАЖНАЯ НАХОДКА: пакет `@tiptap/extension-text-align` был в `package.json` уже с
апдейта `update/2026-08-22/` (проверено: `grep text-align update/2026-08-22/package.json`
— есть), но код его не импортировал почти месяц. Значит на self-hosted сервере
пользователя пакет уже установлен из прошлого `npm install` — в этом апдейте `npm
install` НЕ требуется, только пересборка (`npm run build`). Важно было проверить именно
так (grep по прошлым апдейтам), а не полагаться на память о том, добавляли мы его
недавно или нет.

`RichEditor.tsx`/`src/index.css` — фронтенд без backend, без миграций: полная проверка
свелась к grep на poehali.dev/dev-only (оба чисто, т.к. файл не бэкендный и не Login) и
визуальной проверке рендера (скриншот с hr + pre + text-align на тёмном фоне).

Итог по `update/2026-09-15/` на конец сессии: 44 файла, 2 миграции (V0096, V0097),
4 темы. Собиралось за один день ТРЁМЯ отдельными дозаписями поверх изначальной сборки
(1c6ce2e → +спринты → +редактор) — при каждой дозаписи обязательно повторять полный
блок проверок для ВСЕХ файлов патча, не только для новых (см. запись выше про
tasks/index.py, который откатился при повторном копировании).

✅ 15-16 сентября: собран `update/2026-09-15-3/` (номер задачи, превью ссылок ботам,
обсуждение комментариев — редактирование/реакции/закрепление). ⚠️ НАЙДЕНА ДЫРА: патч
собирался по диапазону файлов конкретных фич (backend/tasks, TaskComments, TaskModal и
т.п.) и НЕ задел `src/index.css`, хотя в диапазон коммитов попал `21e2b01 [FIX] Убрал
лишние бейджи из редактора` (перекрасил инлайн-код в янтарный, осветлил фон блока кода)
— чисто CSS-правка без затронутых "фичевых" файлов, поэтому её пропустили при сборке.
Обнаружилось только 18 сентября, когда пользователь прислал скриншот с боевого сервера:
код серый вместо янтарного. ПРАВИЛО НА БУДУЩЕЕ: при сборке патча по диапазону коммитов
всегда проверять `git log --oneline <start>..HEAD -- src/index.css` (и вообще любой файл
с общими визуальными стилями, который правится нечасто и легко теряется на фоне
"фичевых" файлов с очевидными именами) — не полагаться только на список файлов из
`git diff --stat` тех коммитов, которые считаются "основной темой" патча.

✅ 16-18 сентября: after-the-fact фиксы боевого сервера пользователя (forge.la2era.com) —
не через сборку `update/ДАТА/`, а прямой правкой файлов в `deploy/` по итогам диагностики
на сервере: (1) конфиг Nginx на сервере лежал не по пути из инструкции (`sites-available/
era`), а по `sites-available/forge.la2era.com`, с собственными доработками пользователя
(SSL от certbot, редирект 80→443, отдельный `location /files/` на другой сервер S3) —
собран `update/2026-09-15-3/deploy/nginx-forge-live.conf`, копия ИМЕННО под структуру
этого пользователя, шаблонный `deploy/nginx.conf` было нельзя просто скопировать поверх.
(2) `proxy_pass` внутри `if {}` в location для превью ботам — классический антипаттерн
Nginx ("if is evil"): синтаксис валиден, но `proxy_set_header` внутри `if` запрещён (правка
1), а даже после выноса заголовка наружу regex-группа `$1` внутри `if` вела себя ненадёжно
и `proxy_pass` собирал неверный путь (backend отвечал 404 на запрос, который тот же адрес
напрямую curl'ом отрабатывал верно). Исправлено на надёжную схему: `if` только делает
`rewrite ^/task/(\d+)$ /__og_task/$1 last`, а сам `proxy_pass` живёт в отдельном
`location ~ ^/__og_task/(\d+)$ { internal; ... }` вне `if`. Исправлено СРАЗУ в трёх местах:
`deploy/nginx.conf` (основной шаблон), `update/2026-09-15-3/deploy/nginx.conf` (копия в
патче) и `update/2026-09-15-3/deploy/nginx-forge-live.conf` (версия под боевой сервер
пользователя) — ПРАВИЛО: если чиню баг в `deploy/nginx.conf`, всегда грепать
`grep -rln "^/task/(\\\\d+)\\$" update/*/deploy/*.conf` и чинить каждую копию, не только
основной шаблон. (3) `deploy/server.py` — по просьбе пользователя убрана фраза-заглушка
"ЭРА — внутренний задачник команды" из og:description превью задачи (убрана переменная
`_OG_SITE_DESCRIPTION` и параметр `description` у `_og_html()`, оставлен только заголовок
"#{id} · {title}"); синхронизировано в `update/2026-09-15-3/deploy/server.py`.

✅ 18 сентября: собран НОВЫЙ отдельный патч `update/2026-09-18/` (а не досыпан в
`2026-09-15-3`, т.к. по смыслу это отдельная фронтенд-only тема оформления редактора, не
связанная с номерами задач/комментариями) — две темы: (1) досыпан пропущенный фикс цвета
кода из `21e2b01` (см. запись выше про дыру в `2026-09-15-3`); (2) добавлена подсветка
синтаксиса в блоках кода `RichEditor.tsx` — новые npm-зависимости
`@tiptap/extension-code-block-lowlight` + `lowlight` (лёгкий движок на базе highlight.js,
работает в браузере), `StarterKit` переконфигурирован с `codeBlock: false` (иначе конфликт
имени узла с новым `CodeBlockLowlight`), `lowlight` создан через `createLowlight(common)` —
только распространённые языки, не полный словарь. CSS-классы подсветки (`.hljs-keyword`,
`.hljs-string`, `.hljs-title`/`.hljs-built_in`, `.hljs-number`/`.hljs-type`, `.hljs-comment`
и т.д.) продублированы для `.tiptap-editor .ProseMirror pre` (сам редактор) И `.kb-content
pre` (уже сохранённые статьи/задачи/идеи, рендерятся тем же HTML через
`dangerouslySetInnerHTML`) — иначе подсветка пропадала бы после сохранения контента.
Патч содержит `package.json` целиком для СВЕРКИ (grep на месте), README явно просит не
затирать весь файл пользователя, а дописать две строки в `dependencies` вручную, если в
файле уже есть другие ручные правки. Backend/БД/Nginx не затронуты. 4 файла, без миграций.

❌→✅ ИСПРАВЛЕНО (18 сентября, позже): пользователь прислал скриншот и явно указал, что перепутал
блок кода с ИНЛАЙН-кодом — просил сделать мелкий жёлтый текст крупным/белым, а я по ошибке снял
цветную подсветку синтаксиса с БЛОКА кода (```код```) вместо того, чтобы поправить только инлайн-
код (`код`). Пользователь прямо возразил: «блок кода должен остаться цветным и размеченным».
ИСПРАВЛЕНО: подсветка блока кода (`.hljs-*` классы) возвращена в точности как было; изменён
только `.tiptap-editor .ProseMirror code`/`.kb-content code` (инлайн-код) — `font-size` 0.82em→
0.95em, цвет `--primary` (янтарный) → `hsl(0 0% 96%)` (белый), фон `--secondary` → `hsl(220 10%
26%)` (чуть светлее, чтобы белый текст не терялся). ВЫВОД НА БУДУЩЕЕ: в Tiptap "код" в тексте
почти всегда означает ИНЛАЙН `<code>` (одно слово в кавычках), а "блок кода" — это `<pre><code>`
(тройные кавычки, отдельная строка с своим фоном) — это два РАЗНЫХ CSS-правила
(`.ProseMirror code` vs `.ProseMirror pre code`), и путать их — значит чинить не то, о чём
попросили. При правках оформления кода — переспрашивать/уточнять, если из скриншота не
однозначно очевидно, о каком из двух элементов речь, а не угадывать.

✅ 18 сентября (в тот же день, отдельным патчем-довеском): пользователь заметил, что фикс из
записи выше НЕ попал в `update/2026-09-18/` (там на тот момент был только CSS blocks-only от
предыдущей сессии), и попросил также проверить, что патч по уведомлениям (вкладка «Упоминания»
+ настройки, добавлен в РАЗГОВОРЕ, но не в файлы патча) тоже туда попал. Он не попадал — это
была отдельная сессия, файлы никогда не копировались в `update/`. ПЕРЕСОБРАН `update/2026-09-18/`
полностью (был 4 файла, стало 17): добавлены `backend/auth`, `backend/tasks`, `backend/ideas`,
`backend/patches`, `backend/deadline-reminders`, `backend/notifications` (уведомления +
notify_muted_types), миграция V0101, `src/lib/auth.tsx`, `src/pages/cabinet/CabinetProfile.tsx`,
`src/pages/index/NotificationBell.tsx`. Заодно докопирован ещё один пропущенный фикс того же дня
— `2c13ba8 [FIX] Сохранил данные при закрытии задачи` (confirmClose у ModalOverlay,
CreateTaskModal.tsx/TaskModal.tsx/sharedComponents.tsx) — нашёлся тем же способом (git log
90f8cbb..HEAD по всем файлам, не только по тем, что упоминались в разговоре).
ГЛАВНАЯ НАХОДКА: 4 backend-файла (`auth`, `tasks`, `patches`, `ideas`) СНОВА принесли облачные
S3-дефолты (`bucket.poehali.dev`/`cdn.poehali.dev`) — это уже настолько регулярно повторяющаяся
проблема (см. записи от 6/14/15 сентября про `tasks/index.py`), что стоит считать правилом: ЛЮБОЙ
backend-файл с `_s3_client`/`_public_url`, попадающий в диапазон копирования в `update/`, эталонно
проверять и чинить, даже если конкретно этот файл в этой сессии не редактировался намеренно — он
мог утянуть регресс из общего репозитория с прошлой правки другого разработчика/сессии.
ПРАВИЛО НА БУДУЩЕЕ (закрепление): когда пользователь просит доложить фичу/фикс "в патч за дату X",
всегда сверять ПОЛНЫЙ диапазон коммитов `git log --oneline <последний_коммит_патча>..HEAD` и
`git diff --stat` по ВСЕМ файлам (не только тем, что упоминались в диалоге) — фичи, обсуждённые
в разных сессиях в течение одного дня (код-подсветка, уведомления, confirmClose), легко потерять,
если ориентироваться только на память о разговоре, а не на реальный git log.

✅ 18 сентября (ещё позже, отдельная сессия): в `update/2026-09-18/` ДОБАВЛЕНА 5-я тема —
индикатор непрочитанных идей (коммит `14b09d5`, 9 файлов: `backend/ideas/index.py`, миграция
V0102 (новая таблица `idea_reads`), `src/pages/Index.tsx`, `src/pages/index/Ideas.tsx`,
`IndexMain.tsx`, `IndexTopbar.tsx`, `ideas/IdeasList.tsx`, `ideas/shared.tsx`,
`useBoardData.ts`). Пользователь заранее назвал коммит-сообщение из своей текущей сессии
("Добавь фичу с непрочитанными идеями в патч... убедись что все связанные файлы попали") — не
пришлось искать по диапазону, но проверка `git show --stat 14b09d5` всё равно была сделана, чтобы
не полагаться только на память о том, какие файлы редактировались.
ОЖИДАЕМО ПОВТОРИЛАСЬ находка про S3: `backend/ideas/index.py` уже был скопирован в патч раньше
(для темы уведомлений) с applied self-hosted фиксом `_s3_client`/`_public_url` — свежее
копирование поверх (`cp backend/ideas/index.py update/2026-09-18/...`) СНОВА принесло облачные
`bucket.poehali.dev`/`cdn.poehali.dev`, фикс пришлось накатывать третий раз за день. ПРАВИЛО
ЗАКРЕПЛЕНО ОКОНЧАТЕЛЬНО: копирование файла с `_s3_client`/`_public_url` в `update/` — это ВСЕГДА
двухшаговая операция (1. `cp`, 2. применить self-hosted паттерн), никогда не одношаговая, сколько
бы раз этот файл уже ни чинили раньше в этой же папке патча.
Итог по `update/2026-09-18/` на конец дня: 24 файла, 2 миграции (V0101, V0102), 5 тем. Собирался
в ТРИ подхода в течение дня (сначала только код-подсветка → досыпаны уведомления+confirmClose →
досыпаны непрочитанные идеи), каждый раз с полным повтором проверок (poehali.dev/dev-only/сборка)
для ВСЕХ файлов патча, а не только новых — по правилу, закреплённому выше после случая с
`tasks/index.py` 15 сентября.

⚠️→✅ 18-19 сентября: НАЙДЕН И ИСПРАВЛЕН серьёзный процессный сбой. В течение 18 сентября вместо
одного корректного апдейта было накидано ТРИ отдельных "довеска"-папки (`update/2026-09-18-2/`,
`-3/`, `-4/`) — каждая точечная (таймаут, веб-поиск, стриминг), но собранные с грубыми нарушениями
канона: (1) ЛИШНЯЯ вложенная папка `webapp/webapp/...` внутри каждой — вместо `update/ДАТА/backend/
ai/generate.py` получалось `update/ДАТА/webapp/backend/ai/generate.py`, self-hosted-инструкция
"скопируйте файл в корень проекта" в README на такую структуру не рассчитана; (2) README не всегда
был на своём месте/не собран как единое целое. Пользователь явно указал на оба нарушения и попросил
собрать ВСЁ, что копилось после `update/2026-09-18/`, в ОДИН новый патч по канону.
ИСПРАВЛЕНО: `update/2026-09-18-2/`, `-3/`, `-4/` целиком удалены (mcp__poehali__rm), вместо них
собран единый `update/2026-09-19/` — плоская структура `backend/ai/...`, `deploy/...`,
`src/pages/index/...` (без вложенной `webapp/`), один README на весь патч. Пять тем внутри одного
патча (стриминг self-hosted-only, ход рассуждений, веб-поиск, кеш промпта, таймаут 150с) — все
физически правят одни и те же 2-3 файла (`backend/ai/generate.py`, новый `backend/ai/stream.py`),
поэтому разделять их на отдельные папки-патчи было бы только вредно (пользователю пришлось бы
накатывать 5 конфликтующих версий одного файла подряд).
ПРАВИЛО НА БУДУЩЕЕ (закреплено окончательно): при сборке `update/ДАТА/` папка НИКОГДА не должна
содержать вложенную `webapp/` — файлы кладутся ПРЯМО по путям вида `backend/...`, `src/...`,
`deploy/...`, `db_migrations/...`, зеркалируя структуру корня проекта. Перед тем как сказать
пользователю "патч готов", всегда прогонять `find update/ДАТА/ -type d` и глазами проверять, что
верхний уровень — это `backend/`, `src/`, `deploy/`, `db_migrations/`, а НЕ `webapp/`. Если в разгар
одного дня технической темы (сейчас — раздел AI) появляется несколько мелких доработок одна за
другой в течение нескольких часов/дней — по умолчанию досыпать их В ОДИН и тот же патч дня (как уже
было предписано для многотемных апдейтов 6/15 августа), а не плодить `-2`/`-3`/`-4` версии, если
только темы не полностью независимы физически (разные файлы, разные разделы приложения).

Новая тема в этом же `update/2026-09-19/` — "Ход рассуждений" (миграция V0103, новая колонка
`ai_messages.reasoning`): у thinking-моделей (OpenAI o-серия, DeepSeek-R1, Claude с расширенным
мышлением) AI Tunnel отдаёт `message.reasoning` (нестрим) / `delta.reasoning` (стрим, ПРИХОДИТ
РАНЬШЕ delta.content) — раньше нигде не сохранялось и терялось сразу после ответа. Реализовано и в
`generate.py` (handle_send_message/handle_regenerate), и в `stream.py` — накапливается ОТДЕЛЬНО от
текста ответа, фронт (`AiMessageList.tsx`, компонент `ReasoningPanel`) показывает сворачиваемым
блоком НАД текстом ответа, свёрнут по умолчанию. Не Collapsible из shadcn/ui — простой useState, тот
же паттерн, что уже был у `DiffToggle` в том же файле (в проекте так принято для одноразовых
раскрывающихся блоков).
Кеширование промпта (docs/ai-tunnel-api-reference.md, "Кеширование промпта") реализовано ТОЛЬКО на
`CODE_SYSTEM_PROMPT` (режим «код») — единственный стабильный повторяющийся текст во всём разделе AI:
новые хелперы `_cacheable_text`/`_cacheable_system_message` в `common.py` оборачивают его в
Anthropic-style `cache_control: {"type": "ephemeral"}` (AI Tunnel сам конвертирует под провайдера,
для непонимающих параметр молча игнорируется — НЕ ошибка, см. docs "проигнорирует без ошибки").
Дополнительно `session_id: f'era-chat-{chat_id}'` во всех трёх местах запроса — устойчивая привязка
диалога к одному провайдеру кеша с первого же запроса (без session_id привязка включается только
ПОСЛЕ первого успешного попадания в кэш).
Таймаут — вынесен в константу `CHAT_TIMEOUT_SEC = 150` в generate.py (раньше был голой цифрой `150`
в вызове `_aitunnel_request`, из системы координат непонятно откуда взявшейся) и продублирован в
`stream.py` (самодостаточный модуль, специально НЕ импортирует из generate.py — комментарий в коде
явно требует держать оба числа синхронными при правках). Инфраструктура (gunicorn --timeout 180,
nginx proxy_read_timeout 180s) уже была с запасом — донастройка не понадобилась.
Стандартные проверки при сборке `update/2026-09-19/`: poehali.dev — найден и исправлен ОЖИДАЕМО в
`backend/ai/common.py` (тот же регресс _s3_client/_public_url, что бесконечно повторяется в файлах
с S3 — см. записи от 6/14/15/18 сентября), в остальных файлах патча — только текстовые пояснения в
комментариях про self-hosted-only природу стриминга. Новых pip/npm-зависимостей нет. Схема редактора
в V0103 не хардкожена. Dev-only не затронуты.